# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "4.15.1"
  python_check_max: "3.14"

package:
  name: anyio
  version: ${{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/a/anyio/anyio-${{ version }}.tar.gz
  sha256: 9f28306018cbd6d329e64a36d58256edff76dd996fe423bc957326e578b82a94

build:
  number: 1
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools >=77
    - setuptools-scm >=6.4
  run:
    - idna >=2.8
    - python >=${{ python_min }}
    - typing_extensions >=4.16.0
  run_constraints:
    - trio >=0.32.0
    - uvloop >=0.22.1
    - winloop >=0.2.3

tests:
  - python:
      imports: anyio
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - ${{ python_check_max }}.*
  - files:
      source:
        - tests/
        - pyproject.toml
    requirements:
      run:
        - coverage >=7
        - hypothesis >=4.18.2
        - psutil >=5.9
        - pytest >=9
        - pytest-mock >=3.14
        - pytest-timeout >=2.4.0
        - python ${{ python_min }}.*
        - python-blockbuster >=1.5.23
        - tomli
        - trio
        - trustme >=1.0.0
        - truststore >=0.9.1
        - if: unix
          then:
            - uvloop
          else:
            - winloop
    script:
      env:
        PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
      file: run_test.py

about:
  license: MIT
  license_file: LICENSE
  summary: High level compatibility layer for multiple asynchronous event loop implementations on Python
  homepage: https://pypi.org/project/anyio
  repository: https://github.com/agronholm/anyio
  documentation: https://anyio.readthedocs.io

extra:
  recipe-maintainers:
    - bollwyvl
    - CJ-Wright
    - dhirschfeld
